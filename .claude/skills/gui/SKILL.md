---
name: gui
description: Launch the Cherry GUI prototype
disable-model-invocation: true
---

Launch the Cherry GUI prototype by running:

```bash
cd raytracer && cargo run -p cherry-rs --bin cherry --features gui
```

Run this in the background so the user can continue working while the GUI is open.
